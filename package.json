{
  "private": true,
  "scripts": {
    "postinstall": "node script/get-quasar.js",
    "clean": "rimraf output && rimraf .pulp-cache && rimraf test/tmp",
    "build": "pulp build -- --censor-lib --strict",
    "test-fast": "pulp test --test-path test/src --main Test.Implementation",
    "test": "npm run -s test-fast && pulp test --test-path test/src"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.4",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.6.1",
    "xhr2": "^0.1.4"
  }
}
